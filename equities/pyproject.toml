[project]
name = "equities-autotrader"
version = "0.1.0"
description = "Equities trading strategy backtester"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "yfinance",
    "pyarrow",
    "scikit-learn>=1.3.0",
]

[tool.uv]
dev-dependencies = []
